Review these six full-page mobile screenshots as one page cluster. They are supplied in this order:

1. Bedtime Battles
2. Catnapping
3. Early Rising
4. Nap Transitions
5. Night Wakings
6. Sleep Regressions

The user says the pages feel overwritten and hard to understand. Evaluate only the visible design and page flow.

For each page assess:

- first-screen clarity
- visual hierarchy
- apparent length and density
- section differentiation
- CTA hierarchy
- whether repeated components create useful familiarity or visual monotony
- where a mobile reader is likely to lose the thread

Then recommend:

- one shared mobile page sequence
- which sections should move higher or lower
- which components should be visually compressed
- how the six pages should look related without looking identical
- the three highest-value design changes

Do not judge the accuracy of sleep advice. Do not invent text that is not visible. Separate observations from recommendations. Return concise Markdown. Do not use em dashes.
