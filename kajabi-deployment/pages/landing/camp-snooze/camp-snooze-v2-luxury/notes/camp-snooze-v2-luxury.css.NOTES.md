# camp-snooze-v2-luxury.css - notes

Comments extracted from `camp-snooze-v2-luxury.css`. That file is pasted into Kajabi and ships to
every visitor, so the reasoning lives here instead. Each note names the line it sat above and the
code that followed it, so a note whose anchor no longer exists is a note to re-check.

Regenerate with `node scripts/kajabi/extract-comments.mjs apps/snooze-website/kajabi-deployment/pages/landing/camp-snooze/camp-snooze-v2-luxury/camp-snooze-v2-luxury.css`.

---

## Line 502

```
/* One camp, not five.
```

One camp, not five.
   This was a five-across auto-fit grid while the page listed every upcoming intake. Since
   2026-08-25 the widget renders a single cohort, so the grid is a one-column column that stays
   centred and readable rather than stretching one card across sixty rem.

## Line 559

```
/* The deadline. Rust, the same colour the state label already uses for urgency, and set apart from
```

The deadline. Rust, the same colour the state label already uses for urgency, and set apart from
   the dates above it so a scanner reads "starts" and "closes" as two different kinds of fact.
