# _camp-booking-policy-block.html - notes

Comments extracted from `_camp-booking-policy-block.html`. That file is pasted into Kajabi and ships to
every visitor, so the reasoning lives here instead. Each note names the line it sat above and the
code that followed it, so a note whose anchor no longer exists is a note to re-check.

This file ACCUMULATES. Each extraction appends a dated batch below and never rewrites what is
already here, because once the source is stripped this file is the only copy of that reasoning.
Line anchors are only meaningful within their own batch: an earlier batch was written against an
earlier version of the source and its line numbers have since drifted.

---

## Extracted 2026-08-29

### Line 1

```
CAMP SNOOZE CHECKOUT - booking and refund policy block
```

============================================
     CAMP SNOOZE CHECKOUT - booking and refund policy block
     Deployed as a SECOND code block on all six Camp checkouts, so the
     existing camp checkout block is never rewritten. Self-contained
     inline styles, no dependency on theme CSS.

     Mirrors Terms and Conditions section 5B.
     Pushed live and verified August 29, 2026.

     The AUD payment-plan checkout (theme 2165964553) carries one extra
     bullet about remaining liable for the full fee. Everything else is
     identical to this file.
     ============================================
