# PUBCS01-aud-signature-consult-checkout.html - notes

Comments extracted from `PUBCS01-aud-signature-consult-checkout.html`. That file is pasted into Kajabi and ships to
every visitor, so the reasoning lives here instead. Each note names the line it sat above and the
code that followed it, so a note whose anchor no longer exists is a note to re-check.

This file ACCUMULATES. Each extraction appends a dated batch below and never rewrites what is
already here, because once the source is stripped this file is the only copy of that reasoning.
Line anchors are only meaningful within their own batch: an earlier batch was written against an
earlier version of the source and its line numbers have since drifted.

---

## Extracted 2026-08-29

### Line 1

```
PUBCS01 AUD - Signature Consult checkout, left column text block
```

============================================
     PUBCS01 AUD - Signature Consult checkout, left column text block
     Offer:  2151262016 / wgqokagt   https://app.kajabi.com/admin/offers/2151262016/edit
     Theme:  2166723252              https://app.kajabi.com/admin/themes/2166723252/settings/edit
     Section 1744906803654, block_order is currently EMPTY

     The AUD checkout has NO left-column copy at all. Confirmed August 29,
     2026 via get_theme_content: section 1744906803654 has zero blocks, so
     the AUD buyer sees only the payment form. This file is the AUD twin of
     the USD copy, with the follow-up link pointed at the AUD follow-up
     offer. NOT yet pushed live.
     ============================================
