# Screenshots

Full-page screenshots for all 107 audited pages (one PNG per slug) live in the
external media library (the monorepo pre-commit hook blocks committing binaries):

`snooze-OS-media-library/snooze-product/site-audit-2026-06/screenshots/<slug>.png`

(beside the monorepo, at
`The Sleep Concierge/snooze-OS-media-library/snooze-product/site-audit-2026-06/screenshots/`).

Each `<slug>` matches `pages/<slug>.md` and `data/<slug>.json`.
